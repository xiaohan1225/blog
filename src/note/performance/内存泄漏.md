```js
async initData() {
  await asyncData();
  if (this._isDestroyed) {
    return;
  }
  fn(); // 里面存在闭包导致内存泄漏
}
```
```js
async initData() {
  await asyncData();
  const instance = getCurrentInstance()
  if (this.isUnmounted) {
    return;
  }
  fn(); // 里面存在闭包导致内存泄漏
}
```