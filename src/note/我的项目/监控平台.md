## 前端全链路监控平台


## 总结
- 工程化设计
- SDK 封装
  - 指标采集（空闲时发送，发送队列）
  - 指标上报
- SDK dsn server
  - Kafka 消息队列
    - Topics
    - Partition
  - clickhouse 
    - 列式存储
  - 数据处理聚合统计
- 监控 SaaS 平台
  - 创建应用
  - 监控应用的指标
- CI/CD
  - docker

项目名称： BT一站式前端监控平台
- 角色： 前端开发组长 
- 开始时间：2023-06

项目描述：为了解决线上问题“黑盒”现象及用户报障难以复现的问题，主导设计了公司内部的前端监控SDK及可视化平台。实现了对JavaScript错误、资源加载失败、API请求异常、白屏以及用户核心行为轨迹的采集与报警，覆盖了公司主站及后台管理系统共 10+ 个核心业务，日均处理百万级日志数据。

技术栈：Vue3、TypeScript、Vite、Next.js、Antv、Kafka、Clickhouse等。

<!-- 项目采用 Monorepo 微内核设计，并通过插件化的方式来扩展功能 -->
个人职责：
1、核心SDK开发：从0到1设计轻量级SDK（压缩后<30KB），采用 Monorepo 微内核、插件化架构，支持错误、性能、行为埋点等插件的按需加载，避免影响业务核心逻辑。
2、性能监控：基于Performance API和Web Vitals，采集FCP、LCP、INP、CLS等核心指标，建立性能基线。并支持秒开率、FMP、卡顿等指标。
3、异常监控：设计异常上报体系，能够捕获前端JavaScript错误、资源加载失败、API请求异常等，还支持自定义错误上报，便于开发团队添加业务逻辑中可能遇到的特定错误类型。
4、数据可视化与分析：基于Clickhouse搭建数据存储层，实现了对海量日志的高效存储与查询。利用Vue3 + Antv G2图表库，设计了多维度、多指标的可视化分析页面，如错误趋势分析、性能指标对比、用户行为路径等，帮助开发团队快速定位问题根因。

项目难点：
<!-- 1、SourceMap 反解与错误定位。 生产环境代码经过压缩，上报的堆栈无法直接阅读。通过搭建私有SourceMap存储服务，在监控后台利用source-map库对接收到的错误堆栈进行实时还原，将行列号映射回源码位置，并在告警中直接展示源码错误位置，将排查效率提升60%以上。
2、SPA应用路由切换导致的重复上报与白屏误判。通过在SDK中监听popstate和pushState事件，在路由切换时清空当前页面的错误计数。白屏检测则结合路由变化设置延迟检测，避开页面加载时的空状态，只检测渲染后的真正白屏。
3、海量日志下的采样与计算。对于性能数据等海量日志，在前端SDK端实现动态采样率（根据页面流量或用户等级调整采样率），并在后端引入Flink进行流式计算，实时聚合Web Vitals指标，生成趋势图。 -->

1、首屏加载时间难以量化，广义上的白屏难以采集。通过设计基于MutationObserver的首屏检测指标，将首屏时间检测准确率提高至95%以上。通过设计首屏检测算法，能精准检测到由于后端接口、数据加载、图片和视频加载过程中导致的白屏问题。
2、在做性能监控预警时遇到某个问题，因为性能问题不是短时间能解决掉的，导致预警邮件越发越多，后台拖慢了预警的速度，只能展示2个小时前的数据了，不仅会影响监控系统的性能，还会导致接收邮件的人产生“告警疲劳”，最终忽视真正重要的预警。最终通过引入“告警抑制”与“降噪”机制，实施告警分级与分渠道处理、系统架构解耦、冷热数据分离、限流与熔断等手段，保障监控系统的高可用性。
3、海量日志下的采样与计算。对于性能数据等海量日志，在前端SDK端实现动态采样率（根据页面流量或用户等级调整采样率），并在后端引入Flink进行流式计算，实时聚合Web Vitals指标，生成趋势图。

项目成果：
1、覆盖了95%以上的核心页面，解决了过去线上问题“黑盒”状态，故障发现时效从用户反馈（小时级）缩短至系统告警（分钟级）。。
2、基于监控数据，精准识别到“做题提交答案”接口在弱网环境下失败率高达15%。通过推动后端合并接口及前端请求重试机制，将做题通过率提升至99.5%，显著提升了学员完课体验。
3、结合SourceMap实现错误源码定位，将线上JS异常的排查时间从“需要用户复现步骤”缩短至“直接定位代码行”，前端团队Bug排查效率提升60%。
4、建立页面体验评级模型，通过监控平台发现课程详情页LCP高达3.8秒，在可视化平台分解资源加载和渲染耗时，并通过懒加载、接口优化、渲染策略优化、图片方案升级等方案，使LCP（最大内容绘制）中位数从2.8s降低至1.6s，并通过AB测试监控“选课-下单-支付”核心漏斗页面，优化后订单转化率提升3%。
<!-- 页面体验评级模型（FMP < 1.5s为优，> 3s为差） -->


这是一个非常典型的“告警风暴”问题，不仅会影响监控系统的性能，还会导致接收邮件的人产生“告警疲劳”，最终忽视真正重要的预警。

针对你提到的**“预警邮件越来越多，后台拖慢，只能展示2小时前的数据”**这一现象，核心矛盾在于：**写数据（产生告警日志）的速度远大于读数据（展示、处理告警）的速度，且后台处理逻辑存在瓶颈。**

为了解决这个问题，通常需要从**“告警抑制”、“分级处理”**和**“系统架构解耦”**三个维度入手。以下是具体的解决思路和方案：

### 1. 引入“告警抑制”与“降噪”机制
既然性能问题短期内无法解决，就不能让它无休止地报警。需要让系统变得“智能”一些，减少重复劳动。

-   **状态翻转机制（Flapping）：**
    -   **问题：** 每次检测到性能超标就发邮件，问题恢复又发邮件，反复波动。
    -   **解决：** 引入类似监控系统（如Zabbix/Prometheus）的机制。只有当问题持续一定时间（例如持续5分钟）或连续发生N次探测失败后，才触发警报。问题恢复后，24小时内不再重复发送相同原因的邮件。
-   **沉默周期/告警聚合：**
    -   **问题：** 每5分钟跑一次脚本，每次跑出100个慢SQL，就发100封邮件。
    -   **解决：** 改为将同一时间段内（如30分钟内）的同类错误合并为一封邮件，标题注明“有N个接口发生超时”，而非逐一发送。

### 2. 实施告警分级与分渠道处理
并不是所有的预警都需要通过“邮件”这种重成本的方式通知，也并不是所有预警都需要立即展示在前端。

-   **分级策略：**
    -   **P0/P1（紧急）：** 短信/电话，立即处理。
    -   **P2/P3（警告）：** 邮件，但无需立即展示在前端实时看板，可归档。
    -   **P4（信息）：** 仅入库，供排查问题时查询。
-   **为什么这样做：**
    你提到后台只能展示2小时前的数据，说明查询压力很大。如果将大量P3、P4的日志和预警都塞给前台展示，必然拖慢速度。可以考虑只在前端展示紧急的实时数据，历史数据通过“查询”功能获取。

### 3. 系统架构解耦
你提到后台拖慢，大概率是因为预警产生、邮件发送、数据查询这三者耦合在一起，互相争抢资源。

-   **引入消息队列：**
    -   **现状：** 脚本检测到问题 -> 直接写数据库 -> 直接发邮件。
    -   **优化：**
        1.  脚本检测到问题后，将告警内容丢进**消息队列**，立即返回，不阻塞后续检测。
        2.  由单独的消费者进程从队列中拉取数据，负责写入数据库。
        3.  由邮件服务消费者专门负责发送邮件。
    -   **好处：** 即使邮件发送卡住（比如SMTP服务器响应慢），也不会影响后台写入数据库的速度；即使数据库写入慢，也不会影响检测脚本的运行。
-   **读写分离：**
    -   预警写入数据库（写库）和前端查询展示（读库）分离。
    -   写入的数据通过binlog同步或定时同步到读库。
    -   即使查询很复杂，拖慢了读库，写库依然可以快速接收新的预警，保证数据不积压。

### 4. 冷热数据分离
你提到只能展示2小时前的数据，说明数据库里数据量太大了，查询变慢。

-   **分表/分区：** 按天甚至按小时分区。查询2小时内的数据，只需要扫描最近的分区，速度会很快。
-   **数据归档：** 将超过24小时或48小时的预警数据，自动转移到归档表或归档数据库（甚至是大数据平台）。前端默认只查询热数据，需要看历史数据时，需要加时间筛选并查询慢一些的归档库。

### 5. 限流与熔断
在极端情况下，需要保护监控系统自身。

-   **丢弃策略：** 如果同一时刻产生大量重复预警（比如机房断电，上万台机器同时报失联），系统应该能够识别并丢弃非核心的告警，只发送一条“大面积服务器失联”的汇总通知。

### 总结建议
针对你当前的情况，建议按以下优先级处理：

1.  **立即止血：** 修改发邮件的逻辑，增加**“沉默时间”**。如果同一个IP或同一个接口在最近1小时内已经报过错，则不再发送邮件（或仅增加计数），这能立即减少90%以上的邮件量和后台IO压力。
2.  **恢复性能：** 对告警表进行**数据库分区**（按小时），删除或归档2天前的数据，让前台查询只针对小数据集，尽快恢复到实时查看。
3.  **长期改造：** 引入消息队列，实现告警产生、存储、发送的解耦。

这样做下来，预警系统就不会因为底层业务的性能问题而被拖垮，也能更好地发挥预警作用。
